import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.handykonfigurator',
  appName: 'Handykonfigurator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
